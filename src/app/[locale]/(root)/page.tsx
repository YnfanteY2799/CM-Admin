import { CollidingBeams, LinesBeams, NoiseWaves } from "@/components";

import type { ReactNode } from "react";

export default function Home(): ReactNode {
  return (
    <NoiseWaves>
      <p className="text-white">XD</p>
    </NoiseWaves>
  );
}
